# ============================================================
# 依赖与构建产物 —— 不入库
# ============================================================
# 装了依赖请在本地跑 `pnpm install`，
# 而不是把 node_modules 推上来（那是几百 MB）。
node_modules/
dist/

# ============================================================
# 包管理器 —— 本项目统一用 pnpm
# ============================================================
# 只认 pnpm-lock.yaml。若误用 npm/yarn 装出下列文件，一律忽略，
# 避免「两套锁文件打架」。
package-lock.json
yarn.lock

# ============================================================
# 开发工具产物 —— 不进仓库
# ============================================================
# 牌面校准页由 `pnpm run gen:preview` 生成，是给人看的临时工具，
# 不需要版本管理（而且它已经被构建排除，不会进部署产物）。
public/tile-preview.html

# ============================================================
# 编辑器 / 系统
# ============================================================
.vscode/
.idea/
.DS_Store
Thumbs.db

# ============================================================
# 日志
# ============================================================
*.log
